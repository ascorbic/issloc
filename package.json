{
  "name": "issloc",
  "version": "1.0.0",
  "description": "Cloudflare Worker that updates a DNS LOC record with the ISS position",
  "type": "module",
	"private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "iss",
    "dns",
    "loc"
  ],
  "author": "Matt Kane ",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.11",
    "@types/node": "^24.10.1",
    "@vitest/ui": "^3.2.4",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.51.0"
  }
}
